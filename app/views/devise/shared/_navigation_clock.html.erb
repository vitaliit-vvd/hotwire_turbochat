<!-- right -->
<% if Rails.env.development? %>
  <ul class='flex-fill justify-center'>
    <li class="font-light text-gray-300 text-sm">
      <%= link_to root_path, class: '' do %>
        <%= Time.zone.now.strftime("%A, %D: %k:%M:%S.%L") %>
      <% end %>
    </li>
  </ul>
<% end %>
